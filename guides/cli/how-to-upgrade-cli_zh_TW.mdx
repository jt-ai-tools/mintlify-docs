---
title: "升級您的 Lightdash CLI"
---

您的 Lightdash CLI 版本應與您在 UI 中看到的 Lightdash 版本相符（捲動到螢幕底部即可看到！）。您可以透過執行以下指令來檢查 CLI 版本：

```bash
lightdash --version
```

如果您需要升級 Lightdash CLI 版本，請在終端機中執行以下指令：

```bash
npm install -g @lightdash/cli
lightdash --version
```
