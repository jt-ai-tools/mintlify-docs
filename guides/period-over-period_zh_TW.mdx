---
title: 如何在 Lightdash 中進行期對期 (Period-over-period) 分析
sidebarTitle: 期對期 (PoP)
description: 了解如何在 Lightdash 中進行期對期分析，包含範例與最佳實踐。
---

## 期間比較 (beta)

<Info>
  **此功能目前處於 Beta 階段。**

  我們正在積極測試並改進此功能。
</Info>

我們對生成期對期比較的方式進行了重大改進。您現在可以自定義 PoP 指標的序列類型、標籤、顯示數值標籤等。

要添加期間比較：

1. 選取一個時間/日期維度
2. 選取一個指標 (Metric)
3. 在結果表格中，點擊指標欄位並選擇 **Add period comparison (添加期間比較)**
4. 選擇時間維度與偏移量 (Offset)，然後點擊儲存
5. 將生成的指標添加到您的圖表中

---

期對期分析是評估指標隨時間變化的最常見方式之一。無論您是想查看 **月增率 (MoM)**、**年對年比較 (YoY)** 還是 **週對週表現 (WoW)**，Lightdash 都能讓您直接在儀表板中輕鬆構建這些分析。

本指南將引導您了解在 Lightdash 中進行期對期分析的多個範例。如果您偏好視覺化示範，也可以觀看下方的操作影片。

<div
  style={{
    position: 'relative',
    paddingBottom: '64%',
    height: 0,
  }}
>
  <iframe
    src="https://www.loom.com/embed/be15f8023f754123a177aa1f928477e6?sid=2a08623c-91be-4cef-9497-251b0d4c9185"
    frameborder="0"
    webkitallowfullscreen
    mozallowfullscreen
    allowfullscreen
    style={{
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
    }}
  ></iframe>
</div>

---


## 大數據指標圖 (Big Value Charts)：簡單比較

最快入門的方法是使用 **Big Value** 圖表類型。


### 月增率 (Month-over-month)

- 拉取兩個月份的數據。
- 使用 Big Value 圖表並選擇 **Compare to previous row (與前一行比較)**。
- Lightdash 會自動顯示比較結果。

就這麼簡單！


### 去年同月比較 (Same month, previous Year)

- 拉取約 13 個月份的數據，包含 `MONTH` 與 `MONTH_NUM` 時間維度以及您的指標。
- 建立一個表格計算，過濾僅顯示當前月份與去年同月：

```sql
(extract(month from current_date) - 1) = ${tracks.timestamp_month_num}
```

_此表格計算依賴於 `MONTH_NUM` [時間間隔](/references/dimensions_zh_TW#numeric-options)。_

- 篩選該表格計算值為 `true` 的行。
- 使用 Big Value 圖表並勾選 **Compare to previous row**。


## 趨勢線比較

有時您需要的比單個數值更多 —— 您想看到趨勢如何演變。


### 按年週 (Week of year) 比較

- 在 X 軸添加 **week of year** [時間維度](/references/dimensions_zh_TW#numeric-options) (1-52)。
- 繪製指標（例如：`event_count`）。
- 按 **Year (年份)** 進行分組。
- 這讓您可以跨年度比較趨勢。

參考 Loom 影片中 1:45 處的範例。

<Tip>加分技巧：如果您想提供更直觀的業務視圖，可以使用下方的表格計算為每個週數添加描述，從而得到如下圖表。</Tip> 

<Frame>
  ![](/images/guides/period-over-period/period-over-period-week-of-year.png)
</Frame>


<Accordion title="用於年週描述的表格計算">
```sql
case ${dbt_orders.order_date_week_num}
  when 0  then '0: 12月底/1月初'
  when 1  then '1: 1月初'
  when 2  then '2: 1月中上旬'
  when 3  then '3: 1月中旬'
  when 4  then '4: 1月底/2月初'

  when 5  then '5: 2月初'
  when 6  then '6: 2月中上旬'
  when 7  then '7: 2月中下旬'
  when 8  then '8: 2月底/3月初'

  when 9  then '9: 3月初'
  when 10 then '10: 3月中上旬'
  when 11 then '11: 3月中旬'
  when 12 then '12: 3月中下旬'
  when 13 then '13: 3月底/4月初'

  when 14 then '14: 4月初'
  when 15 then '15: 4月中上旬'
  when 16 then '16: 4月中旬'
  when 17 then '17: 4月底/5月初'

  when 18 then '18: 5月初'
  when 19 then '19: 5月中上旬'
  when 20 then '20: 5月中旬'
  when 21 then '21: 5月中下旬'
  when 22 then '22: 5月底/6月初'

  when 23 then '23: 6月初'
  when 24 then '24: 6月中上旬'
  when 25 then '25: 6月中旬'
  when 26 then '26: 6月底/7月初'

  when 27 then '27: 7月初'
  when 28 then '28: 7月中上旬'
  when 29 then '29: 7月中旬'
  when 30 then '30: 7月底/8月初'

  when 31 then '31: 8月初'
  when 32 then '32: 8月中上旬'
  when 33 then '33: 8月中旬'
  when 34 then '34: 8月中下旬'
  when 35 then '35: 8月底/9月初'

  when 36 then '36: 9月初'
  when 37 then '37: 9月中上旬'
  when 38 then '38: 9月中旬'
  when 39 then '39: 9月底/10月初'

  when 40 then '40: 10月初'
  when 41 then '41: 10月中上旬'
  when 42 then '42: 10月中旬'
  when 43 then '43: 10月底/11月初'

  when 44 then '44: 11月初'
  when 45 then '45: 11月中上旬'
  when 46 then '46: 11月中旬'
  when 47 then '47: 11月中下旬'
  when 48 then '48: 11月底/12月初'

  when 49 then '49: 12月初'
  when 50 then '50: 12月中上旬'
  when 51 then '51: 12月中旬'
  when 52 then '52: 12月底'
  when 53 then '53: 12月底/1月初'
end
```
</Accordion>

### 按月天數與累計總額 (Running totals)

- 比較 6 月 vs 7 月（或任兩個月）。
- 在 X 軸使用 **day of month** [時間維度](/references/dimensions_zh_TW#numeric-options) (1-31)，按月份分組。
- 添加「累計總額 (Running total)」表格計算。
- 選填：使用背景長條圖顯示累進過程。

參考 Loom 影片中 2:30 處的範例。


## 比較任意週

您也可以並排比較任意選定的週。

- 選取特定的週（例如第 2, 8, 12 週）。
- 使用 **week of year** 作為分組維度。
- 套用累計總額查看每週的增長。
- 這顯示了不同週在相同範圍內的趨勢。

參考 Loom 影片中 3:00 處的範例。


## 按月份進行年對年 (YoY) 比較

進行完整的跨月年對年比較：

1. 拉取兩年的月度數據。
2. 在表格計算中使用 `lag()` 函數尋找去年同月的值：

```sql
lag(${event_count}, 12)
```

3. 建立一個百分比變化計算：

```sql
(${event_count} - ${previous_year_value}) / ${previous_year_value}
```

4. 過濾掉 `previous_year_value` 為 `null` 的行。

參考 Loom 影片中 3:45 處的範例。

## 使用參數進行期對期分析

參數允許您在圖表和儀表板中建立動態的期對期比較。

使用者可以輕鬆地在不同日期範圍之間切換，查看指標的追蹤情況 —— 無需重新構建圖表或手動調整過濾器。欲了解更多關於參數的操作，請參考我們的[參數參考指南](/guides/developer/using-parameters_zh_TW)。

本指南將引導您了解在 Lightdash 中使用參數進行期對期分析的範例，您也可以觀看下方的示範教學。

<div
  style={{
    position: 'relative',
    paddingBottom: '51%',
    height: 0,
  }}
>
  <iframe
    src="https://www.loom.com/embed/048e5b84f44e495dba6a3664e98ff037"
    frameborder="0"
    webkitallowfullscreen
    mozallowfullscreen
    allowfullscreen
    style={{
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
    }}
  ></iframe>
</div>

假設您希望使用者選取一個時段（如：昨天、過去 7 天或過去 30 天），然後將當前時段與上一個時段以及一年前的同一時段進行比較。

步驟如下：

<Steps>
  <Step title="設定您的日期範圍 / 期間參數">


#### 日期範圍期間參數語法

    <Tabs>
    <Tab title="dbt v1.9 及更早版本">
    ``` yaml
    models:
      - name: dbt_orders
        description: '此表包含所有已確認訂單及其狀態的資訊'
        meta:
          parameters:
            date_range:
              label: "日期範圍"
              description: "選擇日期範圍"
              options:
                - "yesterday"
                - "last 7 days"
                - "last 30 days"
              default: "last 7 days"
    ```
    </Tab>
    <Tab title="dbt v1.10+">
    ``` yaml
    models:
      - name: dbt_orders
        description: '此表包含所有已確認訂單及其狀態的資訊'
        config:
          meta:
            parameters:
              date_range:
                label: "日期範圍"
                description: "選擇日期範圍"
                options:
                  - "yesterday"
                  - "last 7 days"
                  - "last 30 days"
                default: "last 7 days"
    ```
    </Tab>
    <Tab title="Lightdash YAML">
    ``` yaml
    type: model
    name: dbt_orders

    description: '此表包含所有已確認訂單及其狀態的資訊'

    parameters:
      date_range:
        label: "日期範圍"
        description: "選擇日期範圍"
        options:
          - "yesterday"
          - "last 7 days"
          - "last 30 days"
        default: "last 7 days"
    ```
    </Tab>
    </Tabs>
    
    您也可以參考我們示範 dbt 專案中的 `Date Range` 參數設定：[點此查看](https://github.com/lightdash/lightdash-demo-data-gardening/blob/06ec6e4a87286ec7504d20b10f4b919bd267ebae/dbt-bigquery/models/dbt_orders.yml#L15-L22)。


  </Step>
  <Step title="建立一個維度來分類日期期間">

   接下來，您需要在 `dbt_orders` 模型中建立一個額外維度，使用條件判斷式來計算在 `date_range` 參數中選取的期間。
   
  #### 額外維度語法
    <Tabs>
    <Tab title="dbt v1.9 及更早版本">
    ``` yaml
    models:
     - name: dbt_orders
       description: '此表包含所有已確認訂單及其狀態的資訊'
       meta:
         parameters:
           date_range:
             label: "日期範圍"
             description: "選擇日期範圍"
             options:
               - "yesterday"
               - "last 7 days"
               - "last 30 days"
             default: "last 7 days"
         label: 訂單
       columns:
         - name: order_date
           description: '使用者下單的時戳。'
           meta:
             dimension:
               time_intervals: [
                 'HOUR', 'MINUTE_OF_HOUR_NUM', 'HOUR_OF_DAY_NUM',
                 'DAY', 'DAY_OF_WEEK_INDEX', 'DAY_OF_MONTH_NUM', 'DAY_OF_YEAR_NUM', 
                 'DAY_OF_WEEK_NAME', 'WEEK', 'WEEK_NUM', 
                 'MONTH', 'MONTH_NUM', 'MONTH_NAME',
                 'QUARTER', 'QUARTER_NUM', 'QUARTER_NAME',
                 'YEAR' ]
               type: timestamp
             additional_dimensions:
               order_date_period:
                 description: 由 date_range 參數選取的日期期間（當前期間、上一期間、去年同期或超出範圍）
                 type: string
                 sql: >
                   case
                     -- 當前期間 (current period)
                     when (
                       (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(current_date(), interval 1 day)) or
                       (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(current_date(), interval 7 day) and date_sub(current_date(), interval 1 day)) or
                       (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(current_date(), interval 30 day) and date_sub(current_date(), interval 1 day))
                     )
                     then 'current period'

                     -- 上一期間 (previous period)
                     when (
                       (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(current_date(), interval 2 day)) or
                       (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(current_date(), interval 14 day) and date_sub(current_date(), interval 8 day)) or
                       (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(current_date(), interval 60 day) and date_sub(current_date(), interval 31 day))
                     )
                     then 'previous period'

                     -- 去年同期 (previous year)
                     when (
                       (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(date_sub(current_date(), interval 1 year), interval 1 day)) or
                       (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(date_sub(current_date(), interval 1 year), interval 7 day) and date_sub(date_sub(current_date(), interval 1 year), interval 1 day)) or
                       (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(date_sub(current_date(), interval 1 year), interval 30 day) and date_sub(date_sub(current_date(), interval 1 year), interval 1 day))
                     )
                     then 'previous year'

                     else 'out of range'
                   end
    ```
    </Tab>
    <Tab title="dbt v1.10+">
    ``` yaml
    models:
     - name: dbt_orders
       description: '此表包含所有已確認訂單及其狀態的資訊'
       config:
         meta:
           parameters:
             date_range:
               label: "日期範圍"
               description: "選擇日期範圍"
               options:
                 - "yesterday"
                 - "last 7 days"
                 - "last 30 days"
               default: "last 7 days"
           label: 訂單
       columns:
         - name: order_date
           description: '使用者下單的時戳。'
           config:
             meta:
               dimension:
                 time_intervals: [
                   'HOUR', 'MINUTE_OF_HOUR_NUM', 'HOUR_OF_DAY_NUM',
                   'DAY', 'DAY_OF_WEEK_INDEX', 'DAY_OF_MONTH_NUM', 'DAY_OF_YEAR_NUM',
                   'DAY_OF_WEEK_NAME', 'WEEK', 'WEEK_NUM',
                   'MONTH', 'MONTH_NUM', 'MONTH_NAME',
                   'QUARTER', 'QUARTER_NUM', 'QUARTER_NAME',
                   'YEAR' ]
                 type: timestamp
               additional_dimensions:
                 order_date_period:
                   description: 由 date_range 參數選取的日期期間（當前期間、上一期間、去年同期或超出範圍）
                   type: string
                   sql: >
                     case
                       -- 當前期間 (current period)
                       when (
                         (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(current_date(), interval 1 day)) or
                         (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(current_date(), interval 7 day) and date_sub(current_date(), interval 1 day)) or
                         (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(current_date(), interval 30 day) and date_sub(current_date(), interval 1 day))
                       )
                       then 'current period'

                       -- 上一期間 (previous period)
                       when (
                         (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(current_date(), interval 2 day)) or
                         (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(current_date(), interval 14 day) and date_sub(current_date(), interval 8 day)) or
                         (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(current_date(), interval 60 day) and date_sub(current_date(), interval 31 day))
                       )
                       then 'previous period'

                       -- 去年同期 (previous year)
                       when (
                         (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(date_sub(current_date(), interval 1 year), interval 1 day)) or
                         (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(date_sub(current_date(), interval 1 year), interval 7 day) and date_sub(date_sub(current_date(), interval 1 year), interval 1 day)) or
                         (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(date_sub(current_date(), interval 1 year), interval 30 day) and date_sub(date_sub(current_date(), interval 1 year), interval 1 day))
                       )
                       then 'previous year'

                       else 'out of range'
                     end
    ```
    </Tab>
    <Tab title="Lightdash YAML">
    ``` yaml
    type: model
    name: dbt_orders

    description: '此表包含所有已確認訂單及其狀態的資訊'

    label: 訂單

    parameters:
      date_range:
        label: "日期範圍"
        description: "選擇日期範圍"
        options:
          - "yesterday"
          - "last 7 days"
          - "last 30 days"
        default: "last 7 days"

    dimensions:
      - name: order_date
        description: '使用者下單的時戳。'
        type: timestamp
        time_intervals: [
          'HOUR', 'MINUTE_OF_HOUR_NUM', 'HOUR_OF_DAY_NUM',
          'DAY', 'DAY_OF_WEEK_INDEX', 'DAY_OF_MONTH_NUM', 'DAY_OF_YEAR_NUM',
          'DAY_OF_WEEK_NAME', 'WEEK', 'WEEK_NUM',
          'MONTH', 'MONTH_NUM', 'MONTH_NAME',
          'QUARTER', 'QUARTER_NUM', 'QUARTER_NAME',
          'YEAR' ]
        additional_dimensions:
          order_date_period:
            description: 由 date_range 參數選取的日期期間（當前期間、上一期間、去年同期或超出範圍）
            type: string
            sql: >
              case
                -- 當前期間 (current period)
                when (
                  (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(current_date(), interval 1 day)) or
                  (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(current_date(), interval 7 day) and date_sub(current_date(), interval 1 day)) or
                  (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(current_date(), interval 30 day) and date_sub(current_date(), interval 1 day))
                )
                then 'current period'

                -- 上一期間 (previous period)
                when (
                  (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(current_date(), interval 2 day)) or
                  (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(current_date(), interval 14 day) and date_sub(current_date(), interval 8 day)) or
                  (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(current_date(), interval 60 day) and date_sub(current_date(), interval 31 day))
                )
                then 'previous period'

                -- 去年同期 (previous year)
                when (
                  (${lightdash.parameters.dbt_orders.date_range} = 'yesterday' and date(${order_date}) = date_sub(date_sub(current_date(), interval 1 year), interval 1 day)) or
                  (${lightdash.parameters.dbt_orders.date_range} = 'last 7 days' and date(${order_date}) between date_sub(date_sub(current_date(), interval 1 year), interval 7 day) and date_sub(date_sub(current_date(), interval 1 year), interval 1 day)) or
                  (${lightdash.parameters.dbt_orders.date_range} = 'last 30 days' and date(${order_date}) between date_sub(date_sub(current_date(), interval 1 year), interval 30 day) and date_sub(date_sub(current_date(), interval 1 year), interval 1 day))
                )
                then 'previous year'

                else 'out of range'
              end
    ```
    </Tab>
    </Tabs>

   您也可以參考我們示範 dbt 專案中的 `order_date_period` 維度設定：[點此查看](https://github.com/lightdash/lightdash-demo-data-gardening/blob/06ec6e4a87286ec7504d20b10f4b919bd267ebae/dbt-bigquery/models/dbt_orders.yml#L57-L87)。


  </Step>
  <Step title="使用期間維度構建圖表">

   要建立使用期間參數的圖表，您需要：
   - 選取 `Order date period` 額外維度與 `Order count` 指標。
   - 指定 `order_date_period` 篩選條件不為 `out of range`。
   - 您可以選擇性地過濾掉其他日期群組，例如：如果您想建立一個比較當前期間與上一期間的大數據指標圖，可以過濾掉 `previous year`。

   <Frame>
     ![](/images/guides/period-over-period/period-over-period-example.jpg)
   </Frame>

  </Step>
  <Step title="將圖表添加到儀表板">

    接著您可以將此圖表添加到儀表板。使用者可以從 `Date Range` 參數中選取選項，圖表將自動更新以顯示選取的期間。

  </Step>
</Steps>


### 自定義日期範圍範例

與上述類似，您也可以設定一個自定義日期範圍參數，讓使用者選取特定的開始與結束日期進行分析。

以下是該參數所需的代碼，以及用於自定義日期範圍分類的額外維度代碼：


#### 自定義日期範圍參數配置

<Tabs>
<Tab title="dbt v1.9 及更早版本">
``` yaml
models:
  - name: dbt_orders
    meta:
      parameters:
        custom_range_start:
          label: "自定義日期範圍開始"
          description: "自定義日期範圍的開始日期"
          type: date
        custom_range_end:
          label: "自定義日期範圍結束"
          description: "自定義日期範圍的結束日期"
          type: date
```
</Tab>
<Tab title="dbt v1.10+">
``` yaml
models:
  - name: dbt_orders
    config:
      meta:
        parameters:
          custom_range_start:
            label: "自定義日期範圍開始"
            description: "自定義日期範圍的開始日期"
            type: date
          custom_range_end:
            label: "自定義日期範圍結束"
            description: "自定義日期範圍的結束日期"
            type: date
```
</Tab>
<Tab title="Lightdash YAML">
``` yaml
type: model
name: dbt_orders

parameters:
  custom_range_start:
    label: "自定義日期範圍開始"
    description: "自定義日期範圍的開始日期"
    type: date
  custom_range_end:
    label: "自定義日期範圍結束"
    description: "自定義日期範圍的結束日期"
    type: date
```
</Tab>
</Tabs>


#### 自定義日期範圍額外維度代碼

<Tabs>
<Tab title="dbt v1.9 及更早版本">
``` yaml
columns:
  - name: order_date
    description: '使用者下單的日期。'
    meta:
      dimension:
        type: date
      additional_dimensions:
        order_date_custom_period:
          description: 基於自定義開始與結束日期參數的日期期間分桶
          type: string
          sql: |
            case
              -- 無效的日期範圍
              when ${lightdash.parameters.dbt_orders.custom_range_start} is null
                or ${lightdash.parameters.dbt_orders.custom_range_end} is null
                or ${lightdash.parameters.dbt_orders.custom_range_start} > ${lightdash.parameters.dbt_orders.custom_range_end}
                then 'invalid date range'

              -- 當前期間 (current period)
              when ${order_date_day} >= ${lightdash.parameters.dbt_orders.custom_range_start} 
                and ${order_date_day} <= ${lightdash.parameters.dbt_orders.custom_range_end}
                then 'current period'

              -- 上一期間 (previous period)
              when ${order_date_day} between date_sub(
                ${lightdash.parameters.dbt_orders.custom_range_start},
                interval (date_diff(
                  ${lightdash.parameters.dbt_orders.custom_range_end},
                  ${lightdash.parameters.dbt_orders.custom_range_start},
                  day
                ) + 1) day
              ) and date_sub(
                ${lightdash.parameters.dbt_orders.custom_range_end},
                interval (date_diff(
                  ${lightdash.parameters.dbt_orders.custom_range_end},
                  ${lightdash.parameters.dbt_orders.custom_range_start},
                  day
                ) + 1) day
              )
                then 'previous period'

              -- 去年同期 (previous year)
              when date(${order_date_day}) between date_sub(${lightdash.parameters.dbt_orders.custom_range_start}, interval 1 year)
                and date_sub(${lightdash.parameters.dbt_orders.custom_range_end}, interval 1 year)
                then 'previous year'

              else 'out of range'
            end
```
</Tab>
<Tab title="dbt v1.10+">
``` yaml
columns:
  - name: order_date
    description: '使用者下單的日期。'
    config:
      meta:
        dimension:
          type: date
        additional_dimensions:
          order_date_custom_period:
            description: 基於自定義開始與結束日期參數的日期期間分桶
            type: string
            sql: |
              case
                -- 無效的日期範圍
                when ${lightdash.parameters.dbt_orders.custom_range_start} is null
                  or ${lightdash.parameters.dbt_orders.custom_range_end} is null
                  or ${lightdash.parameters.dbt_orders.custom_range_start} > ${lightdash.parameters.dbt_orders.custom_range_end}
                  then 'invalid date range'

                -- 當前期間 (current period)
                when ${order_date_day} >= ${lightdash.parameters.dbt_orders.custom_range_start} 
                  and ${order_date_day} <= ${lightdash.parameters.dbt_orders.custom_range_end}
                  then 'current period'

                -- 上一期間 (previous period)
                when ${order_date_day} between date_sub(
                  ${lightdash.parameters.dbt_orders.custom_range_start},
                  interval (date_diff(
                    ${lightdash.parameters.dbt_orders.custom_range_end},
                    ${lightdash.parameters.dbt_orders.custom_range_start},
                    day
                  ) + 1) day
                ) and date_sub(
                  ${lightdash.parameters.dbt_orders.custom_range_end},
                  interval (date_diff(
                    ${lightdash.parameters.dbt_orders.custom_range_end},
                    ${lightdash.parameters.dbt_orders.custom_range_start},
                    day
                  ) + 1) day
                )
                  then 'previous period'

                -- 去年同期 (previous year)
                when date(${order_date_day}) between date_sub(${lightdash.parameters.dbt_orders.custom_range_start}, interval 1 year)
                  and date_sub(${lightdash.parameters.dbt_orders.custom_range_end}, interval 1 year)
                  then 'previous year'

                else 'out of range'
              end
```
</Tab>
<Tab title="Lightdash YAML">
``` yaml
dimensions:
  - name: order_date
    description: '使用者下單的日期。'
    type: date
    additional_dimensions:
      order_date_custom_period:
        description: 基於自定義開始與結束日期參數的日期期間分桶
        type: string
        sql: |
          case
            -- 無效的日期範圍
            when ${lightdash.parameters.dbt_orders.custom_range_start} is null
              or ${lightdash.parameters.dbt_orders.custom_range_end} is null
              or ${lightdash.parameters.dbt_orders.custom_range_start} > ${lightdash.parameters.dbt_orders.custom_range_end}
              then 'invalid date range'

            -- 當前期間 (current period)
            when ${order_date_day} >= ${lightdash.parameters.dbt_orders.custom_range_start}
              and ${order_date_day} <= ${lightdash.parameters.dbt_orders.custom_range_end}
              then 'current period'

            -- 上一期間 (previous period)
            when ${order_date_day} between date_sub(
              ${lightdash.parameters.dbt_orders.custom_range_start},
              interval (date_diff(
                ${lightdash.parameters.dbt_orders.custom_range_end},
                ${lightdash.parameters.dbt_orders.custom_range_start},
                day
              ) + 1) day
            ) and date_sub(
              ${lightdash.parameters.dbt_orders.custom_range_end},
              interval (date_diff(
                ${lightdash.parameters.dbt_orders.custom_range_end},
                ${lightdash.parameters.dbt_orders.custom_range_start},
                day
              ) + 1) day
            )
              then 'previous period'

            -- 去年同期 (previous year)
            when date(${order_date_day}) between date_sub(${lightdash.parameters.dbt_orders.custom_range_start}, interval 1 year)
              and date_sub(${lightdash.parameters.dbt_orders.custom_range_end}, interval 1 year)
              then 'previous year'

            else 'out of range'
          end
```
</Tab>
</Tabs>

上述代碼將在 Lightdash UI 中提供如下參數選項，並輸出圖表中顯示的自定義期間標籤：

<Frame>
  ![](/images/guides/period-over-period/custom_range_pop.png)
</Frame>
